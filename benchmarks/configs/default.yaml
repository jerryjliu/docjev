seed: 20260918
repeats: 5
warmups: 3
concurrency: 1
scope: decision
budget_usd: 10
ocr: liteparse
tier: agentic
parser_version: latest
jev_model: jev-1.13.0
baseline_model: gpt-5.6-luna
max_retries: 0
window_size: 8
boundary_threshold: 0.5
min_probability: 0.7
tasks:
  classify:
    manifest: datasets/manifests/classify.json
    rules: examples/classify/rules.yaml
  split:
    manifest: datasets/manifests/split.json
    rules: examples/split/rules.yaml
